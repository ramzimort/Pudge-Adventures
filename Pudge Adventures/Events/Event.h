#pragma once

enum EventType
{
	INITIALIZE_BODY,
	SCALE_BODY,
	SET_ANGLE,
	ROTATE_BODY,
	UPDATE_BODY,
	UPDATE_POSITION,
	MIRROR_OBJECT,
	MIRROR_ARM,
	CAMERA_MOVE,
	INVOKE_CLEAVE,
	INVOKE_HOOK,
	RETURN_HOOK,
	GRAB_HOOK,
	PLAYER_MOVE,
	UPDATE_MOUSE_SCREEN_POSITION,
	UPDATE_MOUSE_WORLD_POSITION,
	ROTATE_TOWARD_POINTER,
	PLAYER_HIT,
	COLLIDE,
	NUM
};

class Event
{
public:
	Event(EventType Type);
	virtual ~Event() {}

	EventType mType;
	float mTimer;
};
